---
description: Automated NestJS Micro-Code Quality Audit
---

# NestJS Best Practices Check Workflow

// turbo-all

1. **Testing Quality Analysis**
   Read `nestjs_best_practices_check/cursor_rules/testing_quality.yaml` and execute the instructions to evaluate testing quality.

2. **Architecture Compliance Analysis**
   Read `nestjs_best_practices_check/cursor_rules/architecture_compliance.yaml` and execute the instructions to evaluate architecture compliance.

3. **Code Standards Analysis**
   Read `nestjs_best_practices_check/cursor_rules/code_standards.yaml` and execute the instructions to evaluate code standards.

4. **DTO Validation Analysis**
   Read `nestjs_best_practices_check/cursor_rules/dto_validation.yaml` and execute the instructions to evaluate DTO validation patterns.

5. **Error Handling Analysis**
   Read `nestjs_best_practices_check/cursor_rules/error_handling.yaml` and execute the instructions to evaluate error handling patterns.

6. **Report Generation - Format**
   Read `nestjs_best_practices_check/cursor_rules/best_practices_format_enforcer.yaml` and execute the instructions to enforce report format.

7. **Report Generation - Generate**
   Read `nestjs_best_practices_check/cursor_rules/best_practices_generator.yaml` and execute the instructions to generate the final report.
